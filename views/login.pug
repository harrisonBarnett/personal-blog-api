extends layout

block content
    form(action='/dashboard/login' method='POST')
        label(for="login") login key:
            input(type="text" name="login" id="login")
        button(type='submit') login